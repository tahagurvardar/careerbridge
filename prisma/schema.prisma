// CareerBridge database foundation.
// Domain models are intentionally deferred until the next product phase.

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}
